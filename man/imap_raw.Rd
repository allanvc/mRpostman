% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imap-raw.R
\name{imap_raw}
\alias{imap_raw}
\title{Criterion constructor function for a verbatim IMAP search fragment}
\usage{
imap_raw(request)
}
\arguments{
\item{request}{A character string with the raw search fragment.}
}
\description{
Wraps any raw IMAP \code{SEARCH} fragment as a search criterion, so that
protocol keys without a dedicated field in
\href{#method-query}{\code{ImapCon$query()}} - vendor extensions such as
Gmail's \code{X-GM-RAW}, sequence sets such as \code{UID 100:200}, or
\code{FUZZY} matches (RFC 6203) - can be combined with the rest of the
search language or with the other criterion constructors. The fragment is
passed to the server exactly as provided (parenthesized if it is not
already), and its keys are not validated locally: an unknown key results
in a server-side \code{BAD} response.
}
\examples{
\dontrun{
# a vendor extension combined with a regular field
res <- con$query(imap_raw('X-GM-RAW "has:attachment smaller:25M"') &
                   flag != "SEEN")
# a UID range in a classic custom search
res <- con$search(request = AND(imap_raw("UID 100:200"),
                                string(expr = "@gmail.com", where = "FROM")),
                  use_uid = TRUE)
}

}
\seealso{
Other custom search:
\code{\link[=AND]{AND()}},
\code{\link{ImapCon}},
\code{\link[=OR]{OR()}},
\code{\link[=before]{before()}},
\code{\link[=filter_stored]{filter_stored()}},
\code{\link[=flag]{flag()}},
\code{\link[=fuzzy]{fuzzy()}},
\code{\link[=larger_than]{larger_than()}},
\code{\link[=modseq]{modseq()}},
\code{\link[=older_than]{older_than()}},
\code{\link[=on]{on()}},
\code{\link[=saved_before]{saved_before()}},
\code{\link[=sent_before]{sent_before()}},
\code{\link[=sent_on]{sent_on()}},
\code{\link[=sent_since]{sent_since()}},
\code{\link[=since]{since()}},
\code{\link[=smaller_than]{smaller_than()}},
\code{\link[=string]{string()}},
\code{\link[=younger_than]{younger_than()}}
}
\concept{custom search}
