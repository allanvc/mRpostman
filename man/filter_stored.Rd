% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-stored.R
\name{filter_stored}
\alias{filter_stored}
\title{Criterion constructor referencing a filter stored on the server}
\usage{
filter_stored(name)
}
\arguments{
\item{name}{A \code{character} string with the name of the filter stored
on the server.}
}
\value{
A search criterion of class \code{imap_search}, to be combined
  into a search statement (see \code{Ops.imap_search}).
}
\description{
Builds a \code{FILTER <name>} search criterion (RFC 5466), which stands for
the search criteria saved on the server under that name through the
ManageSieve protocol. Requires the server \code{FILTERS} capability, which
is checked when the search is executed. Experimental: none of the
reference servers used to validate this package advertises \code{FILTERS},
so the criterion follows the RFC grammar but has not been exercised
against a live server.
}
\examples{
\dontrun{
con$select_folder(folder = "INBOX")
res <- con$search(request = AND(filter_stored("on-the-road"),
                                string(expr = "boss@example.com", where = "FROM")))
}

}
\seealso{
Other custom search:
\code{\link[=AND]{AND()}},
\code{\link{ImapCon}},
\code{\link[=OR]{OR()}},
\code{\link[=before]{before()}},
\code{\link[=flag]{flag()}},
\code{\link[=fuzzy]{fuzzy()}},
\code{\link[=larger_than]{larger_than()}},
\code{\link[=modseq]{modseq()}},
\code{\link[=older_than]{older_than()}},
\code{\link[=on]{on()}},
\code{\link[=saved_before]{saved_before()}},
\code{\link[=sent_before]{sent_before()}},
\code{\link[=sent_on]{sent_on()}},
\code{\link[=sent_since]{sent_since()}},
\code{\link[=since]{since()}},
\code{\link[=smaller_than]{smaller_than()}},
\code{\link[=string]{string()}},
\code{\link[=verbatim]{verbatim()}},
\code{\link[=younger_than]{younger_than()}}
}
\concept{custom search}
