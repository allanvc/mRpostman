% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saved-date.R
\name{saved_before}
\alias{saved_before}
\alias{saved_since}
\alias{saved_on}
\title{Criterion constructor functions for the SAVEDATE extension (RFC 8514)}
\usage{
saved_before(date_char, negate = FALSE)

saved_since(date_char, negate = FALSE)

saved_on(date_char, negate = FALSE)
}
\arguments{
\item{date_char}{A \code{character} string with a date in the IMAP format
\code{DD-Mon-YYYY}, e.g. \code{"17-Apr-2019"}.}

\item{negate}{If \code{TRUE}, negates the search and seeks for "NOT SEARCH
CRITERIA". Default is \code{FALSE}.}
}
\value{
A \code{character} string with the search criterion.
}
\description{
Build the \code{SAVEDBEFORE}, \code{SAVEDON}, and \code{SAVEDSINCE} search
keys, which compare the date a message was saved into the mailbox (the
\code{SAVEDATE} attribute) rather than its internal date or its
\code{Date:} header. They are to be combined with \code{\link{AND}} and
\code{\link{OR}} and passed to \code{ImapCon$search()}. The server must
advertise the \code{SAVEDATE} capability.
}
\examples{
\dontrun{
con$select_folder(folder = "INBOX")
# messages saved into the folder during the last ingestion day
res <- con$search(request = saved_since(date_char = "27-Aug-2026"))
}
}
\seealso{
Other custom search:
\code{\link[=AND]{AND()}},
\code{\link{ImapCon}},
\code{\link[=OR]{OR()}},
\code{\link[=before]{before()}},
\code{\link[=filter_stored]{filter_stored()}},
\code{\link[=flag]{flag()}},
\code{\link[=fuzzy]{fuzzy()}},
\code{\link[=larger_than]{larger_than()}},
\code{\link[=modseq]{modseq()}},
\code{\link[=older_than]{older_than()}},
\code{\link[=on]{on()}},
\code{\link[=sent_before]{sent_before()}},
\code{\link[=sent_on]{sent_on()}},
\code{\link[=sent_since]{sent_since()}},
\code{\link[=since]{since()}},
\code{\link[=smaller_than]{smaller_than()}},
\code{\link[=string]{string()}},
\code{\link[=verbatim]{verbatim()}},
\code{\link[=younger_than]{younger_than()}}
}
\concept{custom search}
