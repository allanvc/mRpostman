% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modseq.R
\name{modseq}
\alias{modseq}
\title{Criterion constructor function for the CONDSTORE extension (RFC 7162)}
\usage{
modseq(value, negate = FALSE)
}
\arguments{
\item{value}{A single non-negative number, the modification sequence to
compare with.}

\item{negate}{If \code{TRUE}, negates the search and seeks for "NOT SEARCH
CRITERIA". Default is \code{FALSE}.}
}
\value{
A \code{character} string with the search criterion.
}
\description{
Builds the \code{MODSEQ} search key, which matches the messages whose
modification sequence is equal to or greater than \code{value}, i.e. the
messages that were added or whose flags changed since the folder had that
\code{HIGHESTMODSEQ} (obtained with \code{ImapCon$status(items =
"HIGHESTMODSEQ")}). It is to be combined with \code{\link{AND}} and
\code{\link{OR}} and passed to \code{ImapCon$search()}. The server must
advertise the \code{CONDSTORE} capability.
}
\examples{
\dontrun{
con$select_folder(name = "INBOX")
last <- con$status(items = "HIGHESTMODSEQ")[["HIGHESTMODSEQ"]]
# ... later, in another run:
changed <- con$search(request = modseq(last + 1))
}
}
\seealso{
Other custom search:
\code{\link[=AND]{AND()}},
\code{\link{ImapCon}},
\code{\link[=OR]{OR()}},
\code{\link[=before]{before()}},
\code{\link[=filter_stored]{filter_stored()}},
\code{\link[=flag]{flag()}},
\code{\link[=fuzzy]{fuzzy()}},
\code{\link[=larger_than]{larger_than()}},
\code{\link[=older_than]{older_than()}},
\code{\link[=on]{on()}},
\code{\link[=saved_before]{saved_before()}},
\code{\link[=sent_before]{sent_before()}},
\code{\link[=sent_on]{sent_on()}},
\code{\link[=sent_since]{sent_since()}},
\code{\link[=since]{since()}},
\code{\link[=smaller_than]{smaller_than()}},
\code{\link[=string]{string()}},
\code{\link[=verbatim]{verbatim()}},
\code{\link[=younger_than]{younger_than()}}
}
\concept{custom search}
